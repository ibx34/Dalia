Dalia programming language. Open to contrib and code cleanup.

[`boot`](/boot) -> Bootstrap compiler written in Haskell

### New(ish?) ideas?
- `m'` and `t'` are "prime forms". open issue for naming ideas or concerns!